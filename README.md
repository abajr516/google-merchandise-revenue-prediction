# Google Merchandise Revenue Prediction

## Executive Summary
Key Findings:
- X
- Y
- Z

---

## Project Overview

Again, XYZ

---

## Environment & Reproducibility
### Reproduce the Environment

```bash
git clone https://github.com/abajr516/google-merchandise-revenue-prediction.git
cd google-merchandise-revenue-prediction

python -m venv .venv
.venv\Scripts\activate

pip install -r requirements.txt
```
### Download the Datasets
The raw dataset is not included in this repository. See [data/README.md](data/README.md) for instructions on obtaining and placing the dataset in the appropriate directory.

### Set up Jupyter

For Jupyter notebooks, register the virtual environment as a Jupyter Kernel:
```bash
python -m pip install ipykernel
python -m ipykernel install --user --name google-merchandise --display-name "Google Merchandise Analytics"
```
Open the project in VS Code, open the desired .ipynb file, and select Google Merchandise Analytics as the Jupyter kernel.

